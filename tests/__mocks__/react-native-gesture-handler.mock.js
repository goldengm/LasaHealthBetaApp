import React from 'react';
import { View } from 'react-native';

const ScrollView = ({ style, contentContainerStyle }) => (
  <View style={style} contentContainerStyle={contentContainerStyle} />
);

export const Swipeable = ({});

export default {
  ScrollView,
};